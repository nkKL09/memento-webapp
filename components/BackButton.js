import React from 'react';

/**
 * Кнопка «Назад» в шапке экрана.
 * В Expo Go не показывается (возвращает null).
 * В Telegram Web App используется системная кнопка в шапке бота (useWebAppNavSync).
 */
export default function BackButton() {
  return null;
}
